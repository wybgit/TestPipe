test_case:
  case_id: mock_device_case
  name: MockDevicePipeline_Basic
  pipeline: MockDevicePipeline
  inputs:
    message: hello mock device
  expected:
    device_stdout: hello mock device
  tags: [mock-device, transfer, device]
  priority: P1
