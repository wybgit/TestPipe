op_name: PathExistsCheck
op_category: check
business_goal: 检查目标路径是否存在
inputs:
  - name: target_path
    type: artifact:path
    description: 目标路径
outputs:
  - name: path_exists
    type: bool
    description: 路径是否存在
scaffold:
  enabled: true
  root_dir: generated
